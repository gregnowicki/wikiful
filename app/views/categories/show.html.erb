<h1>This is the show view for <%= @category.name %></h1>

<% @category.articles.order(created_at: :desc).each do |article| %>
<ul>
  <li>
    <%= link_to article.title, article %>
  </li>
</ul>
<% end %>
